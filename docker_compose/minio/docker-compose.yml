version: '3'

services:
  minio:
    image: quay.io/minio/minio
    command: server /data --console-address ":9001"
    container_name: minio
    networks:
      minio_net_1:
    restart: always
    environment:
      TZ: Asia/Shanghai
      MINIO_ACCESS_KEY: Gathooh88mba5H3y
      MINIO_SECRET_KEY: yFukp3vtIkUNck2XL3s2KOT4wyRPR71G
      MINIO_ROOT_USER: kcserver
      MINIO_ROOT_PASSWORD: kcai1212
      MINIO_REGION: zhuhai-1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ./data:/data
    ports:
      - "9000:9000"
      - "9001:9001"

networks:
  minio_net_1:
    external: true